# Python >= 3.9

pydantic>=2.7          # validation des sorties structurées (§7)
python-dotenv>=1.0     # lecture du .env
httpx>=0.27            # appels HTTP vers Ollama
openai>=1.40           # client du modèle frontier
langfuse>=2.50         # tracing des appels LLM (§9)
streamlit>=1.37        # interface (§12)
reportlab>=4.2         # rendu des PDF depuis pitch_text (§6)
pytest>=8.0            # tests
